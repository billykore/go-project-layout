#!/bin/bash

# Exit immediately if a command exits with a non-zero status,
# an unset variable is referenced, or any command in a pipeline fails.
set -euo pipefail

echo "Executing pre-push hook: Running all unit tests..."

# Run all unit tests
make test

echo "Pre-push checks passed successfully."
